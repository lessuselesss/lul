# Qutebrowser profile definitions
# Each profile can have different settings, bookmarks, etc.
{
  # Default profile is configured in browsers.nix
  # You can add additional profiles here if needed

  # Example: Work profile with different settings
  # work = {
  #   searchEngines.DEFAULT = "https://company.com/search?q={}";
  #   settings = {
  #     content.cookies.accept = "all";
  #   };
  # };
}
